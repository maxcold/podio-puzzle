block menu {
    tag: 'ul'
}

block menu, elem item {
    tag: 'li'

    content: {
        elem: 'link',
        url: this.ctx.url,
        content: this.ctx.content
    }
}

block menu, elem link {
    tag: 'a'

    attrs: {
        href: this.ctx.url
    }
}